const navbar = (
    <nav>
        <h1>B05K19</h1>
        <ul className="nav-list">
            <li>Menu{ServiceWorker.name}</li>
            <li>About</li>
            <li>Contact</li>
        </ul>
    </nav>
);


function navbar(name) => {
    return (
        <h1>{PaymentResponse.name}</h1>
    )
}

ReactDOM.render(
    navbar,
    document.getElementById("root")
);
